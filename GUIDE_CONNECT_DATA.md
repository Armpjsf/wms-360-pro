# 🔌 วิธีเชื่อมต่อข้อมูลจริง (Connect Real Data)

เพื่อให้ Dashboard แสดงข้อมูลจริงจาก Google Sheets แทนข้อมูลตัวอย่าง (Mock Data) คุณต้องทำ 3 ขั้นตอนดังนี้:

## 1. อัปเดต "กุญแจ" (Update Service Key)
ไฟล์ `service-key.json` ปัจจุบันอาจหมดอายุหรือถูกระงับสิทธิ์

1. ไปที่ **[Google Cloud Console](https://console.cloud.google.com/)**
2. ไปที่เมนู **IAM & Admin** > **Service Accounts**
3. คลิกที่ Email ของ Service Account ที่คุณต้องการใช้
4. ไปที่แท็บ **KEYS** ด้านบน
5. คลิก **ADD KEY** > **Create new key**
6. เลือกชนิด **JSON** แล้วกด Create (ไฟล์จะถูกดาวน์โหลดลงเครื่อง)
7. เปลี่ยนชื่อไฟล์นั้นเป็น `service-key.json`
8. นำไปวางทับไฟล์เดิมในโฟลเดอร์ `nextjs-wms`

## 2. แชร์ Google Sheet ให้บอท (Share Access)
บอทต้องได้รับเชิญถึงจะเข้าไปอ่านข้อมูลได้

1. เปิดไฟล์ `service-key.json` ที่คุณเพิ่งวางลงไป
2. หาบรรทัดที่เขียนว่า `"client_email"`
3. **Copy Email** นั้นมา (เช่น: `my-bot@my-project.iam.gserviceaccount.com`)
4. เปิด **Google Sheet** ของคุณ (ทั้งไฟล์หลัก และไฟล์ PO)
5. คลิกปุ่ม **Share (แชร์)** มุมซ้ายบน
6. วาง Email ที่ Copy มาลงไป
7. กำหนดสิทธิ์เป็น **Editor**
8. กด **Send (ส่ง)**

## 3. ตรวจสอบ ID ของ Sheet (Verify IDs)
ต้องบอกรหัสไฟล์ให้ถูกต้อง

1. เปิดไฟล์ `lib/googleSheets.ts` ในโปรเจกต์
2. ตรวจสอบค่า `SPREADSHEET_ID` ว่าตรงกับ ID ใน URL Google Sheet ของคุณหรือไม่
   *(ID คือตัวอักษรยาวๆ ที่อยู่ระหว่าง `/d/` และ `/edit`)*
3. ทำเช่นเดียวกันกับ `PO_SPREADSHEET_ID`

---

## ✅ เสร็จแล้ว!
เมื่อทำครบแล้ว ให้กลับมาที่ Terminal แล้ว:
1. กด `Ctrl + C` เพื่อหยุด Server (ถ้าเปิดอยู่)
2. พิมพ์คำสั่ง `npm run dev` ใหม่
3. เปิดหน้าเว็บดูข้อมูลจริงได้เลย!
